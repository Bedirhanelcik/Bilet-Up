import { CreateEventContent } from "@/features/organizer/components/CreateEventContent";

export const metadata = {
  title: "Create event",
};

export default function CreateEventPage() {
  return <CreateEventContent />;
}
