import { CheckoutContent } from "@/features/tickets/components/CheckoutContent";

export const metadata = {
  title: "Checkout",
};

export default function CheckoutPage() {
  return <CheckoutContent />;
}
